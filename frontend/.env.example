# Base URL of the Nexus backend API.
# Local dev leaves this empty (Vite proxies /api → localhost:8080).
# In production set it to your deployed backend, e.g. https://nexus-api.up.railway.app
VITE_API_URL=
